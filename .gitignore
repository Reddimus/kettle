/target
/kettle-target
/*/kettle-target
**/*.rs.bk
Cargo.lock.orig
.DS_Store

# Per-developer Claude Code state (memory, project metadata). Not
# kettle source. Added after `scripts/release.sh` flagged the
# untracked directory.
.claude/
